# The I of the Dragon
# script for QuickBMS http://quickbms.aluigi.org

get FILES long
for i = 0 < FILES
    get length long
    getdstring NAME length
    get OFFSET long
    get SIZE long
    log NAME OFFSET SIZE
next i
